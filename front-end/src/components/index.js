import Navbar from "./Navbar";
import FootNav from "./FootNav";
import UserList from "./userList";
import UploadFile from "./uploadFile";

// Export components to make it visible into the content layer
// Navbar: Export application Header 
// FootNav: Export application Foote
// UserList: Export the component for users list and add into the content 
// UploadFile: Export the component for uploading files
export{Navbar,FootNav,UserList,UploadFile}